// 1)
var laptop = {  
    color: "silver",
    price: 1500,
    isMac: true,
    write: function() {
        console.log("I'm a " + this.color + " mac that costs $" + this.price);
    }
}

// 2)
var phone = {  
    color: "white",
    price: 1500,
    isIphone: true,
    write: function() {
        console.log("I'm a " + this.color + " phone that costs $" + this.price);
    }
}

// 3)
var monitor = {  
    color: "silver",
    price: 1500,
    isMac: false,
    write: function() {
        console.log("I'm a " + this.color + " monitor that costs $" + this.price);
    }
}

// 4)
var headphones = {  
    color: "silver",
    price: 1500,
    isMac: true,
    write: function() {
        console.log("I'm a " + this.color + " headphone that costs $" + this.price);
    }
}

// 5)
var laptop = {  
    color: "silver",
    price: 1500,
    isMac: true,
    write: function() {
        console.log("I'm a " + this.color + " mac that costs $" + this.price);
    }
}

// 6)
var laptop = {  
    color: "silver",
    price: 1500,
    isMac: true,
    write: function() {
        console.log("I'm a " + this.color + " mac that costs $" + this.price);
    }
}

// 7)
var laptop = {  
    color: "silver",
    price: 1500,
    isMac: true,
    write: function() {
        console.log("I'm a " + this.color + " mac that costs $" + this.price);
    }
}

// 8)
var laptop = {  
    color: "silver",
    price: 1500,
    isMac: true,
    write: function() {
        console.log("I'm a " + this.color + " mac that costs $" + this.price);
    }
}

// 9)
var laptop = {  
    color: "silver",
    price: 1500,
    isMac: true,
    write: function() {
        console.log("I'm a " + this.color + " mac that costs $" + this.price);
    }
}

// 10)
var coffee = {  
    color: "silver",
    price: 1500,
    isMac: true,
    write: function() {
        console.log("I'm a " + this.color + " mac that costs $" + this.price);
    }
}